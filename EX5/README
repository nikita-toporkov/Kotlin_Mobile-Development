Create a counter app with two buttons (“+ and -”) and a text field (in a row) to count up and down.
Change the Text to a text field where you can enter the starting value
Add three counters (in a column) that can count independently
